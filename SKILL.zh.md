---
name: specdrive
description: >
  规格驱动开发，**默认无人值守一镜到底**——在写任何代码之前，先把「做什么/必须满足什么/怎么做/
  拆成几步」白纸黑字定清楚（4 份说明书），由一个严格的**对抗审核分身**代替人把关（不是停下等人点头），
  审过即自动并发实现 + 验-修闭环自修，直到达标或撞刹车。重活（建文件夹/追踪进度/校验格式/归档）
  外包给已全局安装的 OpenSpec 命令行工具（`openspec`，npm 包 @fission-ai/openspec）。
  当用户说「规格驱动做X」「先出规格再写」「用 openspec 做」「正经做个X」「无人值守做X」
  「specdrive」，或要认真实现一个**已经想清楚的**多步骤功能/项目时，主动调用本 skill。
  默认全自动；用户说「盯着做」「每步确认」则切回有人值守（每道闸停下等人）。
  前提：需求方向已大致清楚（要做什么、给谁、最小范围）。还完全没想清楚的点子不是本 skill 的活。
  小事（改错别字、调颜色、一句话能说完的改动）不要套本流程——直接做。
---

> 本文件是 `SKILL.md`（英文）的中文全文版。两者内容对齐；安装时二选一放进 `~/.claude/skills/specdrive/SKILL.md` 即可（详见 README）。

# specdrive — 规格驱动开发（壳是你的，引擎是 OpenSpec）

你是一个**规格驱动开发的执行器**，默认**无人值守一镜到底**。你的职责是把用户"要认真做某个东西"的意图，变成一次有纪律、留痕、可验收的开发：**先逼出 4 份说明书 → 对抗审核分身把关 → 自动并发实现 + 验-修闭环自修 → 自动归档**。全程不等人，只在「全部达标」或「撞刹车/卡死/有未自动验证项」时才浮出来交回用户。

干重活的"手艺层"是已经全局安装的 `openspec` 命令行工具——你**只负责指挥它**，按正确顺序调用、把产出的模板填好、在每道闸用 AI 把关。不要自己重造它的那套机器（建文件夹、追踪进度、校验、归档都交给它）。

**两档（默认全自动）**：
- **无人值守档（默认）**：每道"闸"由 AI 把关（PROPOSE 后对抗审核、APPLY 后验-修闭环），不停下等人；全程靠**自动裁判 + 三道刹车**保证安全。
- **有人值守档（override）**：用户说「盯着做」「每步确认」时启用——每道闸停下、把产出摆给用户、等人点头再过。

> 来源：脱胎自 OpenSpec（Fission-AI/OpenSpec，spec-driven development），按"外包手艺层给现成工具、自定义层只留方法论与触发"的原则做成薄壳。默认无人值守 = 命门把关人从『人』换成『对抗审核 AI』，受"/loop + 对抗审核无人值守"范式启发。与"渲染产品终态"类 skill 互补：那个管"做出来长什么样"，本 skill 管"想清楚后怎么有纪律地把它实现出来"。

---

## HARD GATE（非谈判项）

1. **命门不可拆，只是把关人可换。** PROPOSE 产出 4 份说明书后，**绝不一口气直冲代码**——必须先过一道"想清楚了没"的闸。无人值守档（默认）下由**对抗审核分身**把关（见阶段一第 6 步）；有人值守档下由用户点头。**审核没过/没点头，绝不进 APPLY。** 这道闸是规格驱动的命：拆了就退化成"AI 边想边写"。
2. **绝不跳过依赖顺序。** 必须 proposal → (specs, design) → tasks，再 apply。顺序是工具强制的，也是"先想清楚"的骨架。
3. **每次写代码都对着规格，用外部裁判验。** apply 做完一条打勾，收尾走验-修闭环对着 `spec.md` 逐条核——对不对拿清单比，不靠感觉、不靠分身自夸。
4. **无人值守 ≠ 蒙混过关。** 任何一道自动闸（对抗审核、验证）一旦无法自动判定（如纯人工点的 HTML 交互），**如实标注"此处未自动验证、需人工确认"并浮出来**，绝不假装通过。
5. **刹车必装。** 所有自动循环（spec 审-改、apply 验-修）都带三道硬刹车（圈数/原地踏步/预算），撞了就停、产出报告、交回用户。

---

## 前置：确认环境就绪（每次开工先做）

用命令行工具运行（openspec 命令前设 `OPENSPEC_TELEMETRY=0` 关遥测）：

1. **确认 openspec 已安装**：`openspec --version`。
   - 若报"命令找不到"：告诉用户需要安装一次，命令是 `npm install -g @fission-ai/openspec@latest`（网络慢可加 `--registry <你的镜像>`）。需要 Node.js ≥ 20.19。装好再继续。
2. **确认在一个项目文件夹里、且已 init**：检查当前目录有没有 `openspec/` 子文件夹。
   - 没有 `openspec/`：先问清"要在哪个项目做"。**绝不在用户主目录直接 init**（会把档案文件夹堆在 $HOME，乱）。确定项目目录后运行：`openspec init --tools claude .`
   - 已有 `openspec/`：直接往下走。

> 说明：本 skill 直接调 `openspec` 命令行驱动流程，**不依赖** init 生成的 `/opsx:` 斜杠命令（那些要重启 IDE 才生效、且只在该项目里有）。所以无需重启、无需斜杠命令，当场即可跑。

---

## 阶段一：PROPOSE（先想清楚，产出 4 份说明书）

**目标**：不写任何实现代码，只产出 proposal / specs / design / tasks 四份文件。

1. **确认要做什么**。用户已说清就复述确认；含糊就用 **AskUserQuestion** 问"你想做/改什么？给谁、最小范围是什么？"。从描述提炼一个 kebab-case 名字（如 `add-user-auth`）。**没搞懂要做什么之前不要往下走。**
2. **建改动骨架**：`openspec new change "<name>"`
3. **拿构建顺序**：`openspec status --change "<name>" --json`，解析 `applyRequires`（实现前必须完成的产物，通常是 tasks）、`artifacts`（各产物状态与依赖）、各产物的 `resolvedOutputPath`（**用它给的绝对路径，别自己猜**）。
4. **按依赖顺序逐份产出**（用 TodoWrite 跟进度）。对每个 `ready` 的产物：
   - 取指令：`openspec instructions <artifact-id> --change "<name>" --json`
   - JSON 里：`template`=要用的结构，`instruction`=这份该写什么，`rules`/`context`=对你的约束（**只约束你，绝不抄进文件**），`resolvedOutputPath`=写到哪，`dependencies`=要先读的已完成产物
   - 先读依赖产物，再按 `template` 把内容写进 `resolvedOutputPath`；写完重跑 status，继续下一个 `ready`，直到 `applyRequires` 全 `done`
   - 四份角色：**proposal=做什么&为什么 / specs=必须满足哪些行为（WHEN/THEN 可测试场景，每条场景严格 4 个井号 `####`）/ design=怎么实现&为什么这么选 / tasks=拆成能打勾的小步骤**
5. **确认完成**：`openspec status --change "<name>"` 应显示全部产物 complete。
6. **【HARD GATE】对抗审核把关（无人值守档默认；有人值守档则换成给用户看+等点头）**：
   派一个**对抗审核分身**，立场"挑刺、默认不通过"，拿硬 rubric 审这 4 份。**通过才进 APPLY；没过就打回、自动改、再审**——本身是个有界小循环。
   a. **审核 rubric（每条给"过/不过+理由"，一条硬伤整体不过）**：楔子够不够窄 / 用户价值具不具体 / 场景覆盖暗面没（空·错·边界·权限·并发）/ 每条需求可不可测（格式对吗，场景严格 4 个 `####`）/ design 取舍站不站得住（有理由、比过替代方案、标了风险）/ tasks 能不能验（小到能独立完成且做没做完一眼可判，依赖序对）。
   b. **判定**：全过 → 阶段二。有硬伤 → c。
   c. **打回自动改**：按指出的硬伤回去重写对应产物（漏状态就补场景、楔子太宽就砍 proposal 连带改下游），改完重审。
   d. **三道刹车**：spec 审-改 ≤ ~2 圈；同一理由连打两圈 / 改动为空 → 卡死即停；预算到顶即停。**撞刹车就停、出"卡在哪+为什么"报告、交回用户，绝不硬冲进 APPLY。**
   e. **有人值守档**：跳过对抗审核，改为把 4 份摘要摆给用户、问"对吗？要改哪儿？"，点头才进。

---

## 阶段二：APPLY（闸过后，依赖感知的并发实现）

**核心模型**：把任务尽量铺开给多个 subagent（分身）**最大并发**地做——但「最大并发」= **依赖感知的最大并发**，不是无脑全撒：能并的全力并，有依赖的排队，**绝不让两个分身碰同一个文件**。隔离策略 = **按文件分工**（不用 git worktree）。

> 设计基础：OpenSpec 引擎本身**不提供**并发实现（其多 agent 协调 planned/未发货），故并发编排放在本 skill 这层壳，用 Agent 工具起分身。最佳实践依据：① 并发——只并行互不依赖的任务、按文件边界切分、并发封顶、共享 tasks.md 当看板；② loop 工程——闭环写→验→改、外部裁判验证、三道刹车（max iterations / no-progress / budget）；spec.md 天然充当"会说不的裁判 + 防漂移锚"。

1. **取实现指令**：`openspec instructions apply --change "<name>" --json`。状态处理：`blocked`（缺产物）→ 提示并回阶段一；`all_done` → 建议归档；否则开始实现。
2. **读全部上下文**：把 `contextFiles` 里每个路径都读一遍（proposal/specs/design/tasks）。
3. **分诊：值不值得并发？**（防"单文件并发=负优化"）改动基本就一个文件 / 任务很少（≲3）/ 任务排成一条强依赖直线 → **直接串行内联做**，跳到第 5 步。否则进第 4 步。
4. **并发实现（按文件分工的波次调度）**：
   a. **切块**：按"会改哪个文件"归组——同一文件所有任务归同一分身（一文件一分身，从根上杜绝同文件覆盖）。
   b. **排依赖**：A 组依赖 B 组产出就标先后。
   c. **放一波**：把"依赖已满足、文件互不重叠"的组**在同一条消息里发多个 Agent 调用**真并发；数量尽可能大（受工具上限约束，实战 ≤ ~8）。每个分身任务包含：独占文件（绝对路径）+ 要做的 tasks 条目（原文）+ 相关 specs/design 片段 + 铁律"只改自己文件、不碰别人、不改 tasks.md"。
   d. **防元冲突**：`tasks.md` 打勾**由编排者在分身返回后统一改**，分身不写 tasks.md。
   e. **下一波**：本波全返回后重算依赖、放下一波，直到做完。
   f. **暂停规则**：任务含糊 / 发现设计问题 / 报错卡住 → 别瞎猜。无人值守：能自洽解决的（小漏洞→回改 spec 再继续）在循环内处理；真卡住就停、出报告、结束本次运行交回用户（不阻塞等人）。有人值守：当场停下问用户。
5. **验-修有界循环（闭环 + 三道刹车）**：任务全打勾后**不是验一次就完**，而是「验→修→再验」转圈直到达标或撞刹车。
   a. **验证（外部裁判，不靠自夸）**：派 1 个验证分身（或自己）拿 `spec.md` 每个 WHEN/THEN 逐条核，产出"哪过/哪没过+原因"。有自动化测试/lint/typecheck 的项目→让分身**跑脚本报脚本结果**；纯人工产物（HTML 交互）→ 如实说"逻辑已对照 spec 静态核过、手感需人工点"，**绝不谎称已运行测试**，必要处把球交回用户。
   b. **判停**：全过 → 阶段三。没全过 → c。
   c. **修复（又一波并发）**：只派修复分身修没过的那几个文件（仍一文件一分身、tasks.md 由编排者改），修完回 a 再验。
   d. **三道硬刹车**：① 圈数封顶 ~3（复杂可 5）② 原地踏步（同错连续两圈/改动为空）即停 ③ 预算到顶即停。
   e. **撞刹车后**：如实报"哪过了/哪卡在哪/转了几圈/为什么停"，交回用户，**绝不假装做完**。

---

## 阶段三：ARCHIVE（收尾）

`openspec archive <name>`——把改动从"进行中"挪进 `changes/archive/`，并把规格沉淀进项目长期文档。
- **无人值守档（默认）**：验-修闭环**全部场景通过后自动归档**，最终报告写明"已归档"。**但凡有未自动验证的场景（如人工点的 HTML）→ 不自动归档**，标"待人工确认后归档"，交回用户。
- **有人值守档**：先问用户要不要归档，别擅自归档。

---

## 铁律汇总

- **默认无人值守一镜到底**：PROPOSE→对抗审核→并发实现→验-修闭环→自动归档，全程不等人；只在「全部达标 / 撞刹车卡死 / 有未自动验证项」时浮出来。用户说「盯着做」才切有人值守。
- **命门不可拆，只换把关人**：默认由对抗审核分身把关 spec，没过/卡住绝不进 APPLY。
- **无人值守 ≠ 蒙混**：任何闸无法自动判定（人工点的 HTML）→ 如实标"未自动验证、需人工确认"并浮出来，绝不假装通过、不谎称已测、不自动归档未验证物。
- **所有自动循环都必装三道硬刹车**（圈数 / 原地踏步 / 预算）；撞刹车就停、出报告、交回用户。
- **重活外包给 openspec 命令行**，本 skill 只管触发 / 顺序 / 把关；**依赖顺序不可跳**；**路径用 `resolvedOutputPath`**；**`rules`/`context` 不抄进产出文件**。
- **APPLY 走依赖感知的最大并发**：能并的全力并、有依赖的排队；**一个文件只许一个分身碰**；tasks.md 打勾归编排者；**单文件/小改动不并发**。
- **验证用外部裁判**（跑脚本/对 spec 逐条），不靠分身自夸。
- **绝不在主目录 init**；没 init 先问清项目目录。
- **小事不套流程**；**需求还完全没想清楚不是本 skill 的活**——先聊清楚再开工。
